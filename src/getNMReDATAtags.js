// j'ai du tout commenter pour déposer

/* export class getNMReDATAtags {
// this filters the NMReDATA tags and change the end-of-line character...

    // here loop over the different tags, return the content of the
    // tags whem the tag names start with "NMREDATA_"
    //for{
    // because of problem with some cdk librairies that add end-of-line (ascii 10)
    // in tags, the NMReDATA format uses backslach as "end-of-line". Here we ingnore the 10 and replace the backslash with 10...
    // But we do it only if we detect that the tag contains backslash...
    // this is to allow version 1.0 to be read

    if (count_at_least_one_backslash > 0) {
    curr_tag_content=curr_tag_content.replace(10,"");// this is to ignore the
    curr_tag_content=curr_tag_content.replace("BACK_SLASH",10);// this is to replace backslash with 10.
      //  here put curr_tag_content in an array and return...
    }else{
        //display warning that should use backslash
    }
    //}
}
*/
